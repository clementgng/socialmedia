This folder contains configurations for modules.

keys.js - Stores constants that is used throughout the app
passport.js - Create the type of passport strategy we use for the app. We will use a Jwt Stategy